# v0.5.0 plan — per-frame `dir.ff` (14.6)

**Status:** tagged **v0.5.0** (2026-09-06, requester stamp). Does not replace [DREAMCAST-PORT-PLAN.md](../DREAMCAST-PORT-PLAN.md) bite ids.  
**From:** [v0.4.0](../CHANGELOG.md) (AICA **12**).  
**Why this slice:** requester opened **14.6** after audio. House fire hitch on TOC+`FILE*` is in this tag.

Tag **v0.5.0** when Flycast play-path charges `file_blob` TOC not whole packs, catalog Always+Screen+Prev peak is under 4.5 MB, and the burning house does not `SEEK_SET` every fire frame.

---

## In this version

| Order | Work |
|---|---|
| A | `ff_parse_toc` / `ff_toc_bytes` |
| B | `ff_read_bmp` SEEK_SET (slurp fallback) |
| C | Enter-path TOC + `FILE*`; no whole-pack slurp |
| D | Catalog `file_blob` = TOC; campaign peak ~1.52 MB |
| — | `stopmidi`; `freeze:` idle after feed throw |
| — | Screen loop **tex** stays while that seq is live (#142/#143) |

---

## Out of this version

| Item | When |
|---|---|
| Bite **18** (perf / disc / 240p) | Requester go |
| Hardware / ODE | After this tag |
| DinkC long tail (`sp_sound`, …) | Later |
| Tilesheet slurp / `ts_rgb` catalog over 1.25 MB | Still slurp `tiles/` |
