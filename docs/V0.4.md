# v0.4.0 plan — AICA SFX + music stream

**Status:** tagged **v0.4.0** (2026-08-30, requester stamp). Does not replace [DREAMCAST-PORT-PLAN.md](../DREAMCAST-PORT-PLAN.md) bite ids.  
**From:** [v0.3.0](../CHANGELOG.md) (START + VMU **17**).  
**Why this slice:** requester opened audio **12** after playtest. **14.6** stays gated.

Tag **v0.4.0** when Flycast plays official SFX and streamed MIDI (title `1003.mid`, house `dance.mid`) without a kernel panic or 16 KiB AICA loop on edges.

---

## In this version

| Order | Work | FreeDink |
|---|---|---|
| 1 | **12.1–12.2** WAV→ADPCM + START `load_sound` bank | `snd_sfx_load`; truncate &gt; 65534 samples |
| 2 | **12.3** `playsound` | `SoundPlayEffect`; channel+1 |
| 3 | Editor loop + warp OPEN | `game_place_sprites` / `special_block` |
| 4 | **12.4** `snd_stream` MIDI | `PlayMidi` / `check_midi`; START.c `1003.mid`; mom `dance.mid` |
| 5 | Swap pump | AICA 16 KiB `loop=1` — poll from PCM ring during pack slurps |

---

## Out of this version

| Item | When |
|---|---|
| Per-frame `dir.ff` reads (**14.6**) | Requester full-campaign go |
| Hardware / ODE | After this tag |
| CD-DA (real GD-ROM audio tracks) | MIDI fallback only (`N.mid` for id &gt; 1000) |
| 3D pan / `min`+`plus` variance | May wait |
