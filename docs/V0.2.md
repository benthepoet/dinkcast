# v0.2.0 plan — campaign DinkC

**Status:** tagged **v0.2.0** (2026-08-22). Host items 1–6 landed [#102](https://github.com/benthepoet/dinkcast/pull/102) / [#104](https://github.com/benthepoet/dinkcast/pull/104) / [#105](https://github.com/benthepoet/dinkcast/pull/105) / [#106](https://github.com/benthepoet/dinkcast/pull/106) / [#107](https://github.com/benthepoet/dinkcast/pull/107) / [#108](https://github.com/benthepoet/dinkcast/pull/108). Playtest leftovers [#109](https://github.com/benthepoet/dinkcast/pull/109). Does not replace [DREAMCAST-PORT-PLAN.md](../DREAMCAST-PORT-PLAN.md) bite ids.  
**From:** [v0.1.0](../CHANGELOG.md) (village Flycast snapshot).  
**Why this slice:** village PLAYTEST Open was empty. **14.6** does not unlock new screens. The pole is named FreeDink DinkC / VM holes in [CAMPAIGN-AUDIT.md](CAMPAIGN-AUDIT.md).  
**Canvas:** [docs/canvases/v0-2-plan.canvas.tsx](canvases/v0-2-plan.canvas.tsx).

Requester stamped **v0.2.0**. Host tests lock the grafts. The **Done when** Flycast pictures below are still PLAYTEST Open. Not when `k_fn[]` is 186/186.

---

## Goal

Unblock **scripted** campaign progress past Stonebrook: holes/letter, `goto` loops, `spawn`, `screenlock` arenas, and enemy/NPC targeting (`sp_target` / `sp_follow`). Walking a map **edge** already works (`14.1–14.2`). This version is not “finish Dink Smallwood.”

---

## In this version

Graft FreeDink. Cite the function in each PR. One concern per branch.

| Order | Work | FreeDink | Why |
|---|---|---|---|
| 1 | VM **`goto` / labels** | `locate_goto` in `process_line` | Host #102. Flycast gossip/shop loop still a picture. |
| 2 | **`spawn`** | `dc_spawn` | Host #104. Bombs, dragons, end sequences. |
| 3 | **`load_screen` + `draw_screen`** | `dc_load_screen` → `game_load_screen`; `dc_draw_screen` → `draw_screen_game` | Host #105. `S1-HOLE.c`, `S1-LTR.c`, caves. **Not** edge warp. Fades stay instant. |
| 4 | **`screenlock`** | `dc_screenlock` + `get_hard` clamp | Host #106. Boss/cult/castle cannot walk off the screen. Plan **14.2**. No lock-bar gfx. |
| 5 | **`sp_target` / `sp_attack_wait` / `sp_distance` / `sp_follow` on `BrainSpr`** + `process_target` / `process_follow` | `brain_diagonal.cpp`, `brain.cpp` `process_follow` | Host #107. Deletes `g_target[]`. Pill/dragon ATTACK; Quackers `S1-DUCK.c`. |
| 6 | **`get_sprite_with_this_brain`** (+ rand / next) | `dc_get_sprite_with_this_brain` | Host #108. ~25 combat/cult scripts. `EN-PILL1.c` `die()` unlock. |

**Also in if they fall out of the same grafts** (do not invent extra bites): `sp_frame_delay` when a host test on a stock enemy needs it; `fill_screen` only if a hole/letter script is black without it.

Host tests for each row. PLAYTEST rows only when the requester has seen the picture.

---

## Out of this version

| Item | When |
|---|---|
| Per-frame `dir.ff` reads (**14.6**) | After this slice can *reach* heavy campaign screens; requester full-campaign go |
| AICA **12** (`playsound` / `playmidi`) | After 16 (already landed); next audio tag, not 0.2.0 |
| VMU **17** (`save_game` / `load_game` / `game_exist`) | After this slice; death/continue still broken |
| `count_item` / `free_items` / `kill_this_item` | Follow-up unless a village boot script blocks a 0.2.0 picture |
| `say_stop_xy` / `say_xy` | King / save-machine text; later |
| `indoor[]` / `last_map` map dot | Later |
| editor_type 6/7/8 timers | With **17** |
| Bow hold-charge | Polish; instant-100 stays |
| Full 186 DinkC names | Never a version goal |
| Hardware / ODE | Unproven; not a 0.2.0 gate |

---

## Done when

Requester has seen, in Flycast or on hardware:

1. **Holes / letter** — `S1-LTR.c` fade_up onto 439 confirmed. **`S1-HOLE*`** still a Flycast picture.
2. **`goto`** — a known loop does not fall through (Ethel-area gossip `S1-LG.c` and/or a shop `S2-OUT.c`).
3. **`sp_follow`** — Quackers tracks Dink (`S1-DUCK.c`).
4. **`screenlock`** — Dink cannot walk off a locked arena once that screen is reachable.

`spawn` + brain lookup can be host-locked if the matching Flycast fight is not reachable yet; they still ship in this tag.

---

## PR sequence

Do not stack the whole list on one PR. Suggested branches:

1. `dinkc/goto` — #102 merged (host)
2. `dinkc/spawn` — #104 merged (host)
3. `dinkc/load-draw-screen` — #105 merged (host)
4. `dinkc/screenlock` — #106 merged (host)
5. `brains/target-follow` — #107 merged (host)
6. `dinkc/get-sprite-with-brain`

Human gate after each merge. Visual checks 1–4 above are not a named V7; treat them as PLAYTEST pictures.

---

## Not a visual-gate skip

V1–V6 stay accepted. This version does not open **12**, **17**, or **14.6**.
