# v0.3.0 plan — title menu + VMU save/load

**Status:** in progress. Does not replace [DREAMCAST-PORT-PLAN.md](../DREAMCAST-PORT-PLAN.md) bite ids.  
**From:** [v0.2.0](../CHANGELOG.md) (campaign DinkC host slice).  
**Why this slice:** playtest resume after ELF reset. Remaining campaign pictures (holes/`goto`/Quackers/screenlock, burning-house exit) wait.  
**Canvas:** [docs/canvases/v0-3-plan.canvas.tsx](canvases/v0-3-plan.canvas.tsx).

Requester opened **17** before audio **12**. **14.6** stays gated.

Tag **v0.3.0** when the **Done when** pictures exist in Flycast (or hardware) and host tests lock the grafts.

---

## Goal

Resume a session after reset: official **START** menu can **Load**, and **SAVEBOT.c** can **Save**, both through a compact VMU blob (plan **17.1–17.3**). This is not “finish Dink Smallwood” and not a PC `savegame` file clone.

---

## In this version

Graft FreeDink. Cite the function in each PR. One concern per branch.

| Order | Work | FreeDink | Why |
|---|---|---|---|
| 1 | **17.1 save blob** | `save_game` / `load_game` fields, not the dense PC file | Host file backend. `&player_map`, x/y, items, globals, **sparse** `play.spmap`. **&lt; 8 KB**. |
| 2 | **DinkC save/load** | `dc_save_game`, `dc_load_game`, `dc_game_exist`, `set_save_game_info`; `&savegameinfo` via `decipher_string` + `load_game_small` | `start-2.c` `load()`. Restore is `game_load_screen` + `draw_screen_game`. |
| 3 | **START menu** | `START.c` sprites/procs, not `main()` | Splash (V1) then seq **196** + start-1/2/4. Pad `locate(click)`. No mouse 13, no `load_sound` / MIDI. |
| 4 | **17.2 VMU** | Maple first VMU + `vmu_pkg` | Slots 1–10. Soft-fail if missing. Host tests never require Maple. |
| 5 | **Start pause** | `ESCAPE.c` Continue (no Save); plan Start = pause | Continue / Title. Save is the humming machine only. |

**New game** is `start-1.c` `click()` (`add_item("item-fst")`, `arm_weapon`, `set_mode(2)`) then today’s house load. Dedup the existing fist graft so it does not double-add.

**Quit** (`start-4.c` `kill_game`) returns to splash. Do not `exit()` the ELF.

Host tests for each row. PLAYTEST rows only when the requester has seen the picture.

---

## Out of this version

| Item | When |
|---|---|
| AICA **12** (`playsound` / `playmidi`) | Next audio tag, not 0.3.0 |
| Per-frame `dir.ff` reads (**14.6**) | Requester full-campaign go |
| Campaign Open pictures | After this QoL slice |
| Burning-house exit (`s1-h1-s` freeze/warp) | After this slice |
| Mouse brain 13, `START.c` sound/MIDI, AVI | Never this tag |
| PC `savegame` file compatibility | Never; blob is Dinkcast-only |
| editor_type 6/7/8 timers | Only if a save roundtrip proves we need them |
| Hardware / ODE | Flycast VMU is enough to stamp |

---

## Done when

Requester has seen, in Flycast or on hardware:

1. Splash, then official START wordmark + buttons; D-pad + A (no mouse).
2. New Game still reaches the start house.
3. Talk to the save machine (`save_game`), reset ELF, Load a slot that exists: same `&player_map` + inventory.
4. No VMU: New Game works; Load / `save_game` fail soft.

---

## PR sequence

Do not stack the whole list on one PR.

1. `docs/v0.3-plan` — this file + canvas
2. `bite/17.1-save-blob`
3. `dinkc/save-load`
4. `title/start-menu`
5. `bite/17.2-vmu`
6. `play/start-pause`

Human gate after each merge. Visual checks above are not a named V7; treat them as PLAYTEST pictures.

---

## Not a visual-gate skip

V1–V6 stay accepted. This version does not open **12** or **14.6**.
